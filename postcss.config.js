// filepath: postcss.config.js
// date: October 18, 2024
// description: PostCSS configuration with named export to avoid anonymous default export.

const postcssConfig = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default postcssConfig;
